<% @feedbacks.each do |feedback| %>
  <div class="element">
    <img class="userImage" src="<%= feedback.user.image %>">
    <h3>By: <%= feedback.user.name %></h3>
    <h4>Created At: <%= feedback.created_at %></h4>
    <hr />
    <p> <%= feedback.content %> </p>
  </div>
<% end %>
