<h2>Add New Post %></h2>

<%= form_for @post do |f| %>
  <p><%= f.label :title %>
  <%= f.text_field :title %></p>

  <p><%= f.label :content %>
  <%= f.text_area :content %></p>

  <p><%= f.label :image %>
  <%= f.text_area :image %></p>
  <%= f.submit %>
<% end %>
